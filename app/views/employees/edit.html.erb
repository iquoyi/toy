<% content_for :breadcrumb do %>
  <li class="breadcrumb-item active">Employees <span class="badge bg-warning">UPDATE</span></li>
  <%= back_to employees_path %>
<% end %>

<% content_for :functions do %>
  <%= link_to 'Show', employee_path(@employee.id), class: 'btn btn-info text-white' %>
<% end %>

<%= render 'form', employee: @employee %>
